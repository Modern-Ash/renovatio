# Actors

Project-scoped actors are stored here. User-scoped actors may be loaded from `~/.agora/actors`.
An actor can be a human, AI agent, swarm, service, or automation.

A project actor whose kind is `swarm` may declare `represented-swarm` to link a real local child.
Assignment remains subject to child readiness, role compatibility, cycle checks, and project depth.
