---
schema: "agora/consistency-report/v1"
swarm: "decision-engine"
work: "f0-decision-cartography"
result: "failure"
input-sha256: "65518e215840131ae86540e0ddaa14170a948e3cd7734920948dd93fa8fe7a5d"
actor: "project:owner"
created-at: "2026-09-01T00:21:40.523451Z"
---

# Consistency report

# Consistency review

The two supplied entries are identical and reference the same specification, so they were reviewed as one artifact. The current file hash matches the latest registered hash (`94028fde…`).

## Acceptance criteria

| Criterion | Result | Finding |
|---|---|---|
| Catalog | Success | Contains 38 decision points with all required fields. |
| Categories | Success | All six categories cite fixture examples, including WORKING-STORAGE lifetime for PERSISTENCE. |
| Coupling map | Failure | It is not exhaustive under the clarified requirement covering collaborators, static state, and transitive dependencies. |
| F1 recommendation | Failure | A final scope cut exists, but its supporting frequency data contains a contradiction. |

## Contradictions and coverage gaps

- The coupling map omits the injected `CobolIntermediateModelService` and its transitive `SimpleCobolIrParser` rules, despite semantic-IR parsing materially determining generation output.
- It does not fully map the transitive annotation/report inputs, including identity-projector and validator rules, `SensitiveValueRedactor` policy, and filesystem atomic-replacement behavior.
- The claimed `metadata["source"]` fallback is inaccurate when `filePath` is absent: `Paths.get(fileName)` is executed before fallback resolution and fails for a null path. The fallback is reachable only when a non-null path does not exist.
- Decision #37 reports frequency `8`, but 11 fixture inputs contain PIC declarations with omitted usage. `PicClause` defaults omitted usage to `DISPLAY` for numeric and alphanumeric categories, contradicting the stated fixture-counting method.
- The specification says the Spec Owner resolved and accepted the snapshot, PERSISTENCE example, acceptance criteria, and lifecycle-status questions. The authoritative Agora clarification record leaves those answers blank; criterion statuses remain unmarked, and the only durable consistency evidence is the earlier failure. This conflicts with the document’s own statement that durable lifecycle state is authoritative.

These gaps require `failure` even though the catalog and category coverage are structurally complete.
