---
schema: "agora/consistency-report/v1"
swarm: "decision-engine"
work: "f0-decision-cartography"
result: "failure"
input-sha256: "45df4b806f313c4ddc04d87d0a451fe899b5cf3366d5f750e15521b8745f2f5b"
actor: "project:owner"
created-at: "2026-08-31T23:57:25.848387Z"
---

# Consistency report

# Consistency review

Reviewed the single registered artifact, `docs/specs/decision-model-cartography.md`, non-destructively.

## Acceptance criteria

| Criterion | Result | Finding |
|---|---|---|
| Catalog | Success structurally | The catalog contains 38 decision points and all required columns. Several entries are factually inconsistent, however. |
| Categories | Failure | The PERSISTENCE example claims the CICS path is fixture-exercised, but none of the 13 fixture inputs contains CICS, SQL, VSAM, file-control, or an FD declaration. Thus PERSISTENCE lacks the required real fixture-cited example. |
| Coupling map | Failure | The map is not exhaustive despite claiming to include transitive inputs. It omits parser dialect resolution, workspace filesystem discovery and source contents, `AnalyzeResult` success/message/data, entry names, IR item names, and `MigrationPlanService` consumption of `StubResult` success/generated code/metadata (`outputPath`, `generatedFiles`). |
| F1 recommendation | Failure | An F1 section is present at the end, but its scope is not consistently derived from the stated frequency rule because the underlying frequencies and tier thresholds contradict the fixtures. |

## Material contradictions and gaps

- The document says every fixture has `expected-ir.json`; that file is absent from `goto-irreducible`, `odo-invalid-count`, and `unsupported-construct`.
- Frequency is defined as the number of fixtures exercising a decision, but multiple values are wrong: scaled decimal and signed `S9` occur in only two fixtures, not 4 and 3; only `perform-simple-nested` contains `PERFORM`, not two fixtures; 12 fixtures contain `STOP RUN`/equivalent, not 13; and `compute-decimal-sign` does not exercise numeric `MOVE` as claimed.
- Tier A is defined as frequency ≥8, yet includes decisions reported with frequencies 4 and 3. The F1 rule requires frequency ≥3, but corrected counts would exclude several selected decisions.
- The semantic-IR assessment says there are no IR nodes for level-88 predicates or file/SQL I/O. The repository contains first-class `Level88Condition`, `Level88ConditionReference`, `Db2Statement`, and `FileOperationStatement` models; `CobolDataItem` also directly carries `redefines` and level-88 conditions. This materially understates reusable IR.
- The current integer-mapping description conflates divergent implementations: `CobolTypeMapper` uses Integer/Long/BigDecimal thresholds, while `CobolParsingService` maps every plain numeric value above nine digits to Long.
- The artifact registry records `Content SHA-256` as `none`, so artifact integrity is not verifiable from the durable record. The current file hash is `04f6740fce97fad0608cc358148d6fd6be5881c1f6811a7a62e714b301eb0c07`.

These contradictions affect category coverage, engine-coupling completeness, semantic-IR reuse conclusions, fixture prioritization, and the resulting F1 scope cut.
