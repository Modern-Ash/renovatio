# Swarms

Each directory represents one governed objective. `SWARM.md` is its manifest; work, handoff history,
events, interactions, artifacts, and evidence remain beside it and travel with the Git branch.
