# Implementation plan

Add a read-only aggregation service and endpoint, load it only on the Analysis area, and provide
loading, empty and error states. Verify with compile, contract test, local HTTP request and browser.
