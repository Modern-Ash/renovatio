# Review report

The Equivalence adapter only enumerates workspace descriptors already bounded
by the project adapter. It has no write endpoint, no comparator execution, no
approval action, and does not infer a result from an empty collection. The
existing dashboard and wizard routes are unchanged.
