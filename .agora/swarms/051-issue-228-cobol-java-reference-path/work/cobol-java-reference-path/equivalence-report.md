# Equivalence report

Production end-to-end execution passed for all three reference fixtures with equivalence verification enabled. The generated Java trees match the manifest-declared artifact sets and golden files; repeated execution produced identical generated-tree snapshots and the idempotency run completed without drift.

Known untranslated COBOL constructs are emitted as explicit stable warning action items. No blocking semantic gaps remain for the acceptance fixtures.
