# ArchitectureModel integration

Expose an explicit adapter from the new DomainModel projection to the existing ArchitectureGraph preview contract. The adapter hashes IDs at the boundary, preserves provenance references, maps component kinds, and leaves COBOL parsing/emission unchanged.
