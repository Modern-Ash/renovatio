1. Add `ArchitectureModelAdapter.toGraph` at the architecture boundary.
2. Normalize/hash IDs required by the legacy graph contract and map kinds deterministically.
3. Add focused adapter regression tests and run Maven plus diff hygiene.
