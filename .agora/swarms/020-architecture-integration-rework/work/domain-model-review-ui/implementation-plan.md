1. Define a read endpoint and persistence shape for the canonical DomainModel and its hash.
2. Add a review screen with confidence/provenance and explicit confirmation state.
3. Wire the confirmed payload to the projection client, with validation and regression tests.
