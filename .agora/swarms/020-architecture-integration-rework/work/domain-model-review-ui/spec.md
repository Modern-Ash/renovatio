# DomainModel review UI

The analysis result must expose a versioned, target-neutral `DomainModel` per project. The webapp presents nodes, relations, invariants, confidence and provenance, allows explicit confirmation, and submits only the confirmed model to `/architecture-projection`. No target source code or COBOL text is edited in this step.
