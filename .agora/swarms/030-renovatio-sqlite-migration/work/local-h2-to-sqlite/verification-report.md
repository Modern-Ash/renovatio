# Verification report

SQLite copied `JOBS=31`, `PROJECTS=2`, `PROJECT_PROFILES=2`; all other tracked tables were zero. `RenovatioApiApplication` started successfully against SQLite. The H2 database remains intact.
