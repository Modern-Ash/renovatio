---
schema: "agora/consistency-report/v1"
swarm: "ai-modernization"
work: "llm-runtime-catalog-cache"
result: "failure"
input-sha256: "d00f49688afdf79a44ca2409d3c74b4e2a0c2c5129937d1e5bd92fe36483e94a"
actor: "project:agent"
created-at: "2026-08-30T20:06:53.999787Z"
---

# Consistency report

# Consistency review

## Blocking findings

- **Cache-hit attribution contradiction:** `llm-committed-cache-hit-20260830.md` identifies `tool-20260830t19371788129470z` as a cache hit, while its durable record is a completed `llm-enrichment/enrich` tool-run. The specification says cache hits must not masquerade as cache-miss tool-runs.
- **Promotion workflow contradiction:** The specification defines three commits and says Commit C contains only approval/evidence. The reports and runtime require an additional committed manifest (`d547e8f`), and `CommittedCacheArtifactsLoader` loads that manifest exclusively from Git `HEAD`. The documented three-commit workflow therefore cannot produce the implemented lookup authority.
- **Claude preflight ordering gap:** On a miss, `GovernedEnrichmentService` starts attribution before its lazy provider supplier constructs `AnthropicConfiguration`. A missing `ANTHROPIC_API_KEY` can consequently become an attributed deterministic fallback and cache candidate, contradicting the specified requirement to fail before attribution initialization. Existing tests validate missing configuration but not this ordering.
- **PromptCatalog strictness gap:** The five referenced prompt entries contain the acceptance fields, but the loader checks fallback and schema resources only for existence. It does not validate the fallback resource's version, type, or nonblank manual-action contract, and `deterministic-fallback.v1` is a no-op in the ordered validator path. This falls short of the specification's strict fallback and validator contract.
- **Stale governed artifact registrations:** The registered implementation-plan digest is `3a975a...`, while the current file is `2f9199...`; the registered threat-model digest is `2f8279...`, while the current file is `2d6d4a...`. Material reconciliation changes are therefore not traceable through the registered artifacts.

## Otherwise consistent

The repeated specification artifacts resolve to identical current content; changing test totals reflect chronological rework rather than conflicting results. Provider-neutral wiring, temperature zero, retry/timeout policy, the offline fake, committed cache identity, Agora hash fields, and the `CobolNqlParsingService` rename are supported. The blocking contradictions and coverage gaps above require failure.
