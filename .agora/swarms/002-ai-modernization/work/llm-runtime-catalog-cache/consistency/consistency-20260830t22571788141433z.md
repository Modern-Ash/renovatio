---
schema: "agora/consistency-report/v1"
swarm: "ai-modernization"
work: "llm-runtime-catalog-cache"
result: "success"
input-sha256: "4d84e742d056232c8d437b64c7e33291e9d90162729d7d4318e08443ad513add"
actor: "project:agent"
created-at: "2026-08-30T22:57:13.271979Z"
---

# Consistency report

# Consistency review

The current authoritative v2 contract and implementation are consistent with the work description and all acceptance criteria.

- **Prompt catalog:** Exactly five versioned YAML prompts provide selectors, system text, few-shot examples, versioned schemas, closed validators, and strict deterministic fallbacks.
- **Provider wiring:** Provider-neutral interfaces, Anthropic Messages wiring, environment credentials, 60-second timeout, three-attempt jittered retries, numeric temperature zero, and an offline fake are implemented and tested.
- **Cache:** Canonical complete identities, committed Git-derived authority, A/B/C/D promotion verification, and zero provider construction or attribution on verified hits are implemented. The actual promotion history and manifest-only Commit D satisfy the authoritative lifecycle.
- **Agora attribution:** Durable miss run `tool-20260830t19141788128053z` records the required identifiers and hashes through `llm-enrichment/enrich`; its nonempty successful result reconciles with the promoted envelope without exposing secrets.
- **Service clarity:** Production code uses `CobolNqlParsingService`; the former misleading name is absent from implementation code.

Historical reports and failed reviews describe earlier states. The authoritative v2 specification and explicit correction artifacts supersede their stale lifecycle or classification language while preserving the historically registered bytes. The restored promotion and cache-hit reports match their immutable evidence SHA-256 values, and `agora validate` returns `ok: true`; remaining warnings concern stale historical review inputs rather than integrity failures.

Commit-bound verification is adequate: commit `354d3cb4e12400164f5a24144fd7d674483a8165` has registered full-reactor evidence for 254/254 passing tests, is an ancestor of current `HEAD` `ab302f0585499f6903dc6ec2bee3264744026d19`, and no implementation, prompt, cache-authority, or Maven files changed afterward. A fresh Java 17 full-reactor run at current `HEAD` also passed 254 tests with zero failures, errors, or skips.
