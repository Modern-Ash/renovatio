---
schema: "agora/consistency-report/v1"
swarm: "ai-modernization"
work: "llm-runtime-catalog-cache"
result: "failure"
input-sha256: "4d84e742d056232c8d437b64c7e33291e9d90162729d7d4318e08443ad513add"
actor: "project:agent"
created-at: "2026-08-30T21:57:12.481691Z"
---

# Consistency report

# Consistency review

## Blocking findings

- The authoritative v2 specification requires a successful, commit-bound consistency/review gate before completion. All nine registered consistency checks are failures; the latest failure occurred after owner acceptance. `WORK.md` nevertheless marks every criterion accepted while the work remains `verifying`. The acceptance state therefore contradicts the durable evidence.
- The governed cache-promotion report says Commit C must contain the verified manifest. The v2 specification, implementation plan, threat model, lifecycle correction, and Git history require Commit D alone to contain it. A correction exists, but the registered promotion evidence artifact itself retains the contradictory lifecycle instruction.
- No later successful consistency report supersedes these failures. `agora validate` returns `ok: true`, but that checks structural validity and does not establish semantic consistency.

## Acceptance criteria

- `prompt-catalog`: Technically covered by five versioned entries with system text, few-shot examples, schemas, validators, and strict fallback resources.
- `provider-wiring`: Technically covered by the provider-neutral interface, Anthropic wiring, environment credentials, timeout/retry policy, temperature zero, and offline fake.
- `cache`: Technical behavior is covered, including committed authority and zero provider calls on a verified hit, but promotion evidence is internally inconsistent.
- `agora-attribution`: Technically covered by the durable miss run containing input/output hashes, model, prompt ID, and cache artifact without visible secrets; governance acceptance remains unsupported by a successful consistency gate.
- `service-clarity`: Covered; production code uses `CobolNqlParsingService`, with the former misleading name confined to historical explanatory material.

Repeated identical artifact registrations were normalized by URI, and changing test totals are chronological rather than contradictory. The unresolved promotion and governance inconsistencies require failure.
