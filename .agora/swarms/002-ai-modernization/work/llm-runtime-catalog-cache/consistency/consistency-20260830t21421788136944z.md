---
schema: "agora/consistency-report/v1"
swarm: "ai-modernization"
work: "llm-runtime-catalog-cache"
result: "failure"
input-sha256: "2cf655c42c1b401e3f11aaeec2ae5195a7d9a5bcbc45a2cf3a2e4286ed224959"
actor: "project:agent"
created-at: "2026-08-30T21:42:24.259042Z"
---

# Consistency report

# Consistency review

Blocking contradictions and coverage gaps remain.

## Findings

1. **Invalid promotion artifact:** `docs/reports/llm-cache-promotion-f13b4bf9-20260830.md` identifies Commit D as `d547e8fb5dad53130363354124165003c93fe2c9`, which does not exist. The correction report and Git history identify the actual commit as `d547e8face947e6b5dfa5d6409366c3f54ca5f74`. The current artifacts therefore contradict each other.

2. **Prompt-catalog verification is incomplete:** the authoritative v2 specification requires a versioned output schema and every malformed class to fail closed. `PromptCatalogLoader` checks only that the schema resource exists, and its test helper successfully uses unversioned `schema.json`. Tests also omit duplicate prompt IDs, blank system text, malformed few-shot examples, empty validators, and invalid fallback diagnostic codes, despite the plan requiring every rejection class to be tested.

3. **Temperature-zero evidence is missing:** production code sets Anthropic `temperature` to zero, but `ProviderRuntimeTest` never inspects the outbound request body. Test reports claiming verification of the complete Anthropic request policy therefore overstate coverage.

4. **Governance is incomplete:** durable `WORK.md` remains `implementing`, has an empty `satisfied-criteria` list, and every registered consistency review is a failure. No successful, commit-bound readiness/review/overall approval gate is recorded, although the authoritative specification requires these before completion. The existing owner approval covers the single cache promotion, not overall acceptance.

## Criterion assessment

- `prompt-catalog`: **gap** — the five concrete entries and fallbacks exist, but version enforcement and rejection-class evidence are incomplete.
- `provider-wiring`: **partially supported** — provider-neutral interface, Anthropic wiring, environment credentials, timeout, retry, offline fake, and temperature-zero implementation exist; request-policy test coverage is incomplete.
- `cache`: **gap** — the implementation and actual A/B/C/D history support committed cache hits without provider construction, but the registered promotion report contains a false Commit D identifier.
- `agora-attribution`: **technically supported but not durably accepted** — the governed miss record contains the required hashes, model, prompt ID, artifact URI, and no visible secrets; the work criterion remains unsatisfied.
- `service-clarity`: **supported** — the implementation is named `CobolNqlParsingService`; `LlmIntegrationService` remains only in explanatory historical documentation.

Repeated v1-spec, plan, threat-model, and catalog records were normalized as duplicate registrations. Historical test-count changes are chronological rather than contradictory, but they do not resolve the blocking findings above.
