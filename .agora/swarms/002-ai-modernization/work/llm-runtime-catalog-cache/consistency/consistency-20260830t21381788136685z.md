---
schema: "agora/consistency-report/v1"
swarm: "ai-modernization"
work: "llm-runtime-catalog-cache"
result: "failure"
input-sha256: "f685db1d84dd51855df65cbfdd293ca2973129c14ef4cd06b1b94dd89c6cf50c"
actor: "project:agent"
created-at: "2026-08-30T21:38:05.664963Z"
---

# Consistency report

# Consistency review

Blocking contradictions and coverage gaps were found.

- **Specification authority:** The implementation plan names `docs/specs/llm-runtime-catalog-cache-v2.md` as authoritative, but no v2 specification is included in the supplied artifacts. The v1 URI is repeated many times, and its final occurrence is truncated at `committed-cache-index.`, so the artifact set cannot establish one complete authoritative specification.
- **Promotion lifecycle:** The specification, plan, and threat model require four commits: A for the envelope, B for the index, C for approval/evidence, and D containing only the verified manifest. `llm-cache-promotion-f13b4bf9-20260830.md` instead says Commit C must persist the verified manifest. `llm-committed-cache-hit-20260830.md` then identifies a separate manifest commit, confirming the contradiction.
- **Integrated verification:** The latest supplied commit-bound full-reactor report tests commit `1401309a77b473dd15836d10c9a595b7f3f73ce3`. The supplied promotion-history and annotated-IR semantic-validation reports describe later focused changes but do not bind them to a subsequent full-reactor commit. Therefore the final combined implementation represented by the artifacts lacks integrated, commit-bound verification.
- **Governance:** Durable Agora state remains `implementing`, lists no satisfied criteria, and contains only failed consistency reviews. `agora validate` is structurally successful, but that does not resolve these semantic inconsistencies.

## Criterion assessment

- `prompt-catalog`: supported by the catalog, referenced prompt entries, and strict-loader reports.
- `provider-wiring`: supported by provider-neutral, Claude-policy, configuration, retry/timeout, and offline-fake evidence.
- `cache`: **failure** because the promotion authority is internally contradictory and final integrated verification is missing.
- `agora-attribution`: governed miss evidence exists, but the criterion is not durably accepted in the work record.
- `service-clarity`: supported by the `CobolNqlParsingService` rename and focused test evidence.

The promotion contradiction, incomplete specification artifact, missing authoritative v2 artifact, and verification gap require an overall failure.
