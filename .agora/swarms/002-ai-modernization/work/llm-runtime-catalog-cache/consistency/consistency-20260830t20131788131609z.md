---
schema: "agora/consistency-report/v1"
swarm: "ai-modernization"
work: "llm-runtime-catalog-cache"
result: "failure"
input-sha256: "c89b157c0ec449a8f0575866fa5ed4863e5e0e87b525cef11efe9e4dffee5de3"
actor: "project:agent"
created-at: "2026-08-30T20:13:29.760310Z"
---

# Consistency report

# Consistency review

The artifacts contain material contradictions and coverage gaps.

## Blocking findings

- **Promotion lifecycle contradiction:** The authoritative specification and implementation plan require four commits—A, B, C, and manifest Commit D. The threat model still states a three-commit workflow, while the promotion report says Commit C must persist the verified manifest. Actual history places the manifest in `d547e8f` as Commit D, confirming those artifacts are stale or contradictory.
- **Commit-D enforcement gap:** Runtime verification records only Commit A/B/C and merely requires C to be an ancestor of `HEAD`. It does not bind the manifest to a distinct Commit D or prove that D contains only the generated manifest, despite the specification’s explicit acceptance scenario.
- **Fallback contract gap:** Fallback resources declare a stable `diagnosticCode`, but `CatalogFallbackFactory` ignores that declared value and emits the provider failure diagnostic instead. Therefore the strict catalog fallback resource does not fully produce the documented fallback shape.
- **Artifact integrity gap:** The supplied artifact set includes multiple instances of the same specification URI, with the final instance truncated at `## 9. Lifecycle requ`. This prevents all supplied specification artifacts from representing one complete authoritative document.
- **Governance and evidence gap:** The work remains `implementing`, `satisfied-criteria` is empty, and every registered consistency review is a failure. The latest implementation rework is supported only by evidence with `tested-commit: null`; the last commit-bound full-reactor report predates the current implementation tip.

## Criterion assessment

- `prompt-catalog`: **failure** — required entries exist, but the declared fallback diagnostic contract is not honored by runtime rendering.
- `provider-wiring`: **success** — provider-neutral interface, Claude transport, environment configuration, timeout/retry policy, temperature zero, and offline fake are supported.
- `cache`: **failure** — a committed cache hit is demonstrated, but the specified four-commit promotion invariant is not fully enforced.
- `agora-attribution`: **partial** — governed miss attribution and durable reconciliation are implemented and demonstrated, but current-tip evidence and criterion satisfaction are not finalized.
- `service-clarity`: **success** — `CobolNqlParsingService` replaces the misleading service and remains NQL-specific.

`agora validate` reports structural validity, but structural validity does not resolve these semantic contradictions and coverage gaps.
