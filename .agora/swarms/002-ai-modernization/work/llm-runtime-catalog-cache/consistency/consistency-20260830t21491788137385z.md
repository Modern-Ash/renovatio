---
schema: "agora/consistency-report/v1"
swarm: "ai-modernization"
work: "llm-runtime-catalog-cache"
result: "failure"
input-sha256: "d7d82e9443087d66fb79b7381d557bfcc5fdde20f95b30f7735d8564a27defd3"
actor: "project:agent"
created-at: "2026-08-30T21:49:45.268465Z"
---

# Consistency report

# Consistency review

## Blocking gaps

- Durable Agora state is incomplete: `WORK.md` remains `implementing`, `satisfied-criteria` is empty, all five criteria stop at `implemented`, and revision 1 remains open. No successful consistency review or overall Spec Owner acceptance exists. The sole approval covers cache promotion A/B hashes, not completion of the work.
- The artifact ledger is stale for `llm-cache-promotion-f13b4bf9-20260830.md`: it registers SHA-256 `8cbda276…`, which identifies the earlier report containing an invalid Commit D. The corrected artifact supplied and currently present has SHA-256 `ea755832…` but is not registered. Thus the durable artifact record contradicts the corrected report.
- Every registered consistency check, including the latest, records `failure`. Later corrections and passing tests address its technical findings, but no subsequent successful, commit-bound consistency/review gate establishes acceptance as required by the authoritative v2 specification.

## Acceptance assessment

- `prompt-catalog`: Technically covered. Five versioned entries declare all required fields; strict loader and negative tests cover versioning, duplicate IDs, selectors, examples, schemas, validators, resources, and fallback contracts.
- `provider-wiring`: Technically covered. Provider-neutral interface, Anthropic Messages transport, environment credentials/model, 60-second timeout, three-attempt jittered retry policy, offline fake, and serialized numeric temperature zero are implemented and tested.
- `cache`: Technically covered. Complete canonical identity, Git-HEAD authority loading, committed index/manifest verification, A/B/C/D promotion checks, and zero provider or runtime-attribution calls on verified hits are supported.
- `agora-attribution`: Technically covered. The durable miss run records prompt ID, provider/model, input/output/schema/cache hashes, dispositions, and cache artifact URI without visible secrets; promotion reconciliation validates the run and result.
- `service-clarity`: Covered. Production and test code use `CobolNqlParsingService`; the former misleading name appears only in historical explanatory material.

## Artifact normalization and verification

Repeated v1-spec, plan, threat-model, catalog, promotion-report, and cache-hit-report entries were treated as duplicate registrations of their respective URIs. The v1 specification correctly delegates authority to v2. Historical test-count changes are chronological, and correction reports explicitly supersede earlier classification and promotion-language errors.

A fresh focused Java 17 reactor run completed successfully with exit code 0. This confirms the current technical implementation but does not resolve the stale artifact digest or missing successful governance gates.
