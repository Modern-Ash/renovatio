---
schema: "agora/tool-result/v1"
run: "tool-20260904t13581788541131z"
status: "completed"
exit-code: 0
result-kind: "work-item"
---

# Tool result tool-20260904t13581788541131z

## Standard output

    {"assignees":[],"body":"## Contexto\n\nParte del epic \"motor de migración parametrizable por decisiones\". Depende de la Fase 0 (cartografía).\n\nHoy `ProjectEntity` tiene `javaOutputPath`, `javaPackage`, `javaArchitecture` como campos sueltos. Esta fase los promueve a un **`migration-profile` estructurado y versionado**, y agrega un **registro de decisiones** (impedancia caso por caso) con sugerencia asistida por LLM que el usuario confirma o edita. **No se toca el motor de emisión** — los defaults reproducen byte a byte el comportamiento actual.\n\n## Alcance\n\n### Módulos nuevos\n- **`renovatio-profile`**: modelo `MigrationProfile` v1, JSON schema versionado (`schemas/migration-profile-v1.json`), carga/validación/merge YAML↔objeto, cálculo de \"profile efectivo\" = `defaults ∘ profile ∘ decisiones{CONFIRMED,OVERRIDDEN}`.\n- **`renovatio-decisions`**: modelo `DecisionPoint` (id estable = hash de coordenadas semánticas, category, location, question, options[], defaultOption, chosenOption, source `HEURISTIC|LLM|USER`, confidence, rationale, evidence, status `AUTO|SUGGESTED|CONFIRMED|OVERRIDDEN`), store, `DecisionResolver`.\n\n### Modelo `MigrationProfile` v1\n```\nschemaVersion, extensions\ntarget:       { language: JAVA(only active) | NODE | PYTHON, languageVersion }\narchitecture: { style: TRANSACTION_SCRIPT | LAYERED_MVC | HEXAGONAL, moduleGrouping }\nruntime:      { framework }\npersistence:  { defaultStrategy: JPA | SPRING_DATA_JDBC | IN_MEMORY, transactionBoundary }\nstyle:        { numericPolicy: BIGDECIMAL | SCALED_LONG, nullability, errorHandling, naming }\nllm:          { enabled, suggestDecisions, maxSuggestionsPerRun }\n```\n\n### `DecisionSuggestionService` (en `renovatio-llm`)\nEnvuelve `PromptRuntime` / `PromptOutputValidator` / cache / `DeterministicFallback` para puntos de decisión (no residuo de código). Prompts nuevos: `decision.numeric`, `decision.control-flow`, `decision.data-shape`, `decision.persistence`, `decision.naming`, cada uno con JSON schema de salida estricto `{ chosenOption, confidence, rationale }`. LLM caído o salida inválida → `DeterministicFallback` = `defaultOption`, `source=HEURISTIC`, flag `llmFailed` para telemetría. Nunca bloquea el wizard.\n\n### API (`renovatio-api`)\n- `GET/PUT /api/projects/{id}/profile` (valida vs schema; 422 con violaciones)\n- `GET /api/projects/{id}/decisions?category=&minConfidence=&status=`\n- `PATCH /api/projects/{id}/decisions/{did}` `{ chosenOption }` → CONFIRMED/OVERRIDDEN (400 si fuera de options[])\n- `POST /api/projects/{id}/decisions:bulk-confirm` `{ minConfidence }`\n- `GET /api/projects/{id}/profile:effective`\n- Migración DB: `javaOutputPath|javaPackage|javaArchitecture` → `MigrationProfile` v1; campos legacy deprecados un release.\n\n### Flujo\nAnalyze (existente) además emite `DecisionPoint`s: heurística determinista primero → confidence; si `confidence < umbral` y `profile.llm.suggestDecisions` → `DecisionSuggestionService` → `SUGGESTED`. Cache `CacheIdentity(semanticIrHash + promptVersion + profileHash)`. Plan/Apply leen \"profile efectivo\" vía adapter fino hacia `JavaGenerationService` / `MigrationPlanService`.\n\n### Web app (`renovatio-ui`)\n- **Step \"Target\"** (entre `StepFolder` y `StepAnalyze`): selects language/architecture/framework/persistence + toggle \"sugerencias con IA\" + preview textual del layout (estático por estilo en F1).\n- **Step \"Decisions\"** (entre `StepAnalyze` y `StepPlan`): tabla filtrable por category/confidence/status; badge \"IA\"/\"heurística\"; rationale expandible; acciones confirmar / editar opción / \"confirmar todas ≥ 0.8\".\n- `wizard/api`: clientes de los endpoints nuevos.\n\n## Riesgos\n1. **Ids de decisión inestables** entre re-análisis → el usuario pierde confirmaciones. Mitigación: id por coordenadas semánticas, no textuales; test dedicado.\n2. **Schema v1 corto** → versionar pronto. Mitigación: `schemaVersion` obligatorio + campo `extensions` libre desde el día 1.\n3. **El adapter profile→motor** puede filtrar más acoplamiento del esperado. Mitigación: F0 lo mapea antes.\n\n## YAGNI\n- Sin emitters Node/Python (solo enums declarados, deshabilitados en UI).\n- Sin transformación real de arquitectura (F3).\n- Sin clasificador de accesos a datos (F4) — `persistence.defaultStrategy` global.\n- Sin motor de reglas configurable.\n- Sin perfiles reutilizables entre proyectos.\n\n## Verificación\n1. `renovatio-profile`: round-trip YAML↔objeto; validación schema (válidos/inválidos); precedencia `default < profile < decisión`.\n2. `renovatio-decisions`: `DecisionResolver` precedencia; ids estables ante re-análisis; bulk-confirm por umbral.\n3. `DecisionSuggestionService` con LLM stubbeado: salida válida → SUGGESTED; basura → fallback; cache hit no re-llama.\n4. API: contract tests de los 5 endpoints + test de migración de los 3 campos legacy.\n5. UI: los dos steps nuevos (render, submit, bulk-confirm) siguiendo `StepDiff.test.jsx`.\n6. **Caracterización**: profile = defaults → salida generada byte-idéntica a la de hoy (harness de #122). `mvn clean install` verde; server MCP sin regresión.\n\n## Gobernanza\nBajo Agora, spec-driven + TDD, un ciclo spec→plan→implementación. Insumo: `docs/specs/decision-model-cartography.md` de F0.\n\n---\n🤖 Generated with [Claude Code](https://claude.com/claude-code)\n","createdAt":"2026-08-31T22:45:49Z","labels":[{"id":"LA_kwDOPowW0s8AAAACJUCmtw","name":"enhancement","description":"New feature or request","color":"a2eeef"},{"id":"LA_kwDOPowW0s8AAAACJUP9zg","name":"codex","description":"","color":"ededed"}],"milestone":null,"number":146,"state":"CLOSED","stateReason":"COMPLETED","title":"F1 · Capa de decisión: renovatio-profile + renovatio-decisions + sugerencia LLM + Steps Target/Decisions","updatedAt":"2026-09-01T12:25:24Z","url":"https://github.com/Modern-Ash/renovatio/issues/146"}

## Standard error

    (empty)
