---
schema: "agora/tool-result/v1"
run: "tool-20260904t13581788541130z"
status: "completed"
exit-code: 0
result-kind: "work-item"
---

# Tool result tool-20260904t13581788541130z

## Standard output

    {"assignees":[],"body":"## Contexto\n\nRenovatio hoy sólo se usa como servidor MCP (JSON-RPC 2.0, HTTP :8082 o stdio). No hay CLI ni GUI. Para correr una migración COBOL→Java hay que `mvn clean install`, arrancar el jar y llamar `cobol.analyze/plan/apply/diff` con JSON crudo desde un cliente MCP, leyendo `ToolCallResult` en JSON. Los outputs quedan en `<workspace>/generated-java-stubs/*.java`; los guardrail \"manual action items\" en `build/reports/renovatio/manual-action-items.json` sin render.\n\nObjetivo: bajar la barrera de adopción para (a) analistas COBOL / devs mainframe que no saben Java/Maven/MCP y quieren ejecutar y revisar migraciones, y (b) líderes técnicos que necesitan métricas, complejidad, plan y estado de gates para reportar.\n\n**Veredicto de viabilidad: VIABLE (riesgo medio-bajo).** El core ya está desacoplado del protocolo (`LanguageProviderRegistry.routeToolCall()`); CLI y API REST son adaptadores hermanos, no reescrituras. Ya hay render human-facing parcial (`/reports/html|pdf`, PDFBox) y el modelo `ManualActionItem` ya es una \"lista de tareas para un humano\".\n\n## Alcance — 3 fases\n\n### Fase 1 — `renovatio-cli` (módulo Maven nuevo, picocli)\nUn binario con subcomandos 1:1 con las tools, llamando al core in-process:\n```\nrenovatio analyze <path> [--dialect IBM] [--scope '**/*.cbl']\nrenovatio metrics <path>\nrenovatio plan    <path> [--strategy incremental] [--framework spring-boot]  -> planId\nrenovatio apply   <planId> [--dry-run/--no-dry-run] [--out <dir>]            -> runId\nrenovatio diff    <runId> [--format unified|semantic|both]\nrenovatio review  [--report build/reports/renovatio/manual-action-items.json]\nrenovatio report  [--html out.html | --pdf out.pdf]\nrenovatio serve   [--http | --stdio]\n```\n- Arranca `ApplicationContext` headless, obtiene `LanguageProviderRegistry`.\n- Salida legible por default; `--json` para scripting.\n- `review` renderiza los manual action items como checklist priorizada por severidad (reason → requiredHumanAction → acceptanceCondition), ya redactada vía `SensitiveValueRedactor`.\n- Previo: consolidar en `CobolLanguageProvider` como fachada única; deprecar `CobolMcpToolsProvider` / `CobolProvider`.\n\n### Fase 2 — `renovatio-api` (REST + jobs async + persistencia)\nMódulo Spring Boot web, REST orientado a UI (no JSON-RPC):\n- `POST /api/projects`, `GET /api/projects/{id}`\n- `POST /api/projects/{id}/jobs {op, params}` -> jobId; `GET /api/jobs/{id}` (status/progress/result); `GET /api/jobs/{id}/events` (SSE)\n- `GET /api/projects/{id}/plan` · `/runs/{runId}/diff` · `/metrics` · `/action-items`\n- `POST /api/action-items/{id}/status {reviewStatus}`\n- Persistencia H2/SQLite embebido: `Project`, `Job`, `MigrationPlanSnapshot`, `RunSnapshot`, `ActionItem`. Envolver `MigrationPlanService` (hoy estado en memoria) en un `PersistentPlanStore`.\n- Jobs async: `ThreadPoolTaskExecutor` / `JobService` con cola.\n- Gating por rol reusando `AccessRole` / `ReportAccessService`.\n\n### Fase 3 — `renovatio-ui` (SPA: wizard + dashboard)\nVite + React/Svelte, build estático servido por `renovatio-api` (CSP estricto, self-contained).\n- **Wizard (analista):** elegir carpeta → Analyze (progreso SSE) → métricas+complejidad → configurar Plan → Apply dry-run → diffs lado a lado → revisar action items (marcar accepted/rejected) → Apply real / exportar reporte.\n- **Dashboard (líder):** tarjetas de métricas (LOC, complejidad ciclomática, copybooks, procedimientos complejos), estado de los 4 guardrail gates por programa, action items por severidad, timeline de jobs, export HTML/PDF.\n- Sin terminología MCP/OpenRewrite en la UI; tooltips por término COBOL.\n\n## Riesgos / dependencias\n1. Estado en memoria en `MigrationPlanService` — necesita persistencia (trabajo no trivial mayor).\n2. Guardrails no cableados: `GuardrailGateRunner` / `ManualActionItemWriter` no tienen callers en `main/`. El dashboard de gates depende de la integración previa (#122, `characterization-guardrails`).\n3. 3 fachadas COBOL coexisten (`CobolLanguageProvider` / `CobolMcpToolsProvider` / `CobolProvider`) + nombres de tools inconsistentes vs README — elegir UNA antes de envolver.\n4. Toolchain: gates de compilación/caracterización requieren Maven + JDK 17 en la máquina del usuario — el instalador debe resolverlo (jlink / imagen / contenedor).\n5. Operaciones largas (analyze/apply en repos grandes) — jobs async + progreso, no request-response.\n6. Seguridad: el dashboard muestra diffs y código — reusar `SensitiveValueRedactor` y gating por rol desde el arranque.\n\n## YAGNI\n- No multi-tenant / auth compleja (single-user local primero).\n- No editor de código embebido (sólo lectura de diffs).\n- No reimplementar el server MCP (`renovatio serve` lo delega).\n- No tocar la lógica de generación/transpilación.\n\n## Esfuerzo\nCLI ~1-2 sem · API ~2-3 sem · SPA ~3-4 sem · MVP usable ~6-9 semanas (1 persona), en fases entregables.\n\n## Verificación\n1. CLI: `renovatio analyze specs/1-cobol-python-migration/examples/p1` → métricas coherentes con `ir_prog1.json`; `plan`→`apply --dry-run`→`diff` encadenan por id; `review` renderiza un `manual-action-items.json` de ejemplo.\n2. API: `POST /api/projects` + job `analyze` → polling llega a `succeeded`; reiniciar proceso y verificar persistencia; SSE emite progreso.\n3. UI: wizard completo end-to-end contra fixture p1 en browser; marcar action item \"accepted\" persiste; dashboard muestra métricas + gates; export HTML/PDF.\n4. No regresión: `mvn clean install` verde; server MCP arranca igual (jar y `--stdio`).\n\n## Gobernanza\nTrabajo bajo Agora (swarm nuevo `003-adoption-tooling` o dentro de `002-ai-modernization`), spec-driven + TDD, una fase = un ciclo spec→plan→implementación.\n\n---\n🤖 Generated with [Claude Code](https://claude.com/claude-code)\n","createdAt":"2026-08-30T16:55:57Z","labels":[{"id":"LA_kwDOPowW0s8AAAACJUCmtw","name":"enhancement","description":"New feature or request","color":"a2eeef"}],"milestone":null,"number":130,"state":"CLOSED","stateReason":"COMPLETED","title":"CLI + GUI para adopción por usuarios no técnicos (wizard + dashboard)","updatedAt":"2026-08-31T18:03:53Z","url":"https://github.com/Modern-Ash/renovatio/issues/130"}

## Standard error

    (empty)
