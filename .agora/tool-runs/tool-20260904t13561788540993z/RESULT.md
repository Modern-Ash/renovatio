---
schema: "agora/tool-result/v1"
run: "tool-20260904t13561788540993z"
status: "completed"
exit-code: 0
result-kind: "work-item"
---

# Tool result tool-20260904t13561788540993z

## Standard output

    {"assignees":[],"body":"## Objetivo\n\nConvertir Renovatio de una migración cableada (COBOL→Java, arquitectura fija, persistencia fija) en un **motor de migración parametrizable por decisiones**:\n\n- El usuario elige **lenguaje destino** (Java; luego Node.js / Python), **arquitectura** (transaction-script, MVC en capas, hexagonal), **framework** y **estrategia de persistencia**.\n- El sistema reduce la **impedancia** del mundo plano COBOL hacia lenguajes modernos **decisión por decisión**: política numérica (COMP-3), `88`-levels, `REDEFINES`/`OCCURS DEPENDING ON`, `GO TO`/fall-through, agrupación de copybooks, scope de `WORKING-STORAGE`, archivos `FD`, `EXEC SQL`, CICS, etc.\n- Para las decisiones finas de baja confianza, el sistema **sugiere** una opción con justificación (heurística o LLM gobernado) que el usuario **confirma o edita**.\n- Todo **versionado, determinista y auditable**: mismo input + mismo profile + mismas decisiones = misma salida. El LLM solo interviene en el residuo, con salida validada contra JSON schema, cacheada y atribuida (Agora).\n\nSucesora de #130 (CLI + API + SPA de adopción). Estrategia consistente con el motor determinista existente y \"LLM solo en el residuo\".\n\n## Concepto central\n\nUn artefacto declarativo `migration-profile` (v1, versionado) + un registro de `DecisionPoint` (opción, fuente, confianza, rationale, evidencia, estado) que juntos son la única fuente de verdad de \"hacia qué migro y cómo\". Alimenta CLI, API y UI por igual.\n\nCorte arquitectónico:\n```\nCOBOL → cobol-ir → semantic-ir (neutral) → ArchitectureProfile (IR→IR) → TargetModel → TargetEmitter/<lenguaje>\n                          ↑                        ↑                            ↑\n                   migration-profile + decisiones (con sugerencia LLM gobernada)\n```\n\n## Fases\n\n- [ ] **F0 · Cartografía** (spike) — #145 — inventario de puntos de decisión + acoplamiento del motor actual. Sin código productivo. Entregable: `docs/specs/decision-model-cartography.md`.\n- [ ] **F1 · Capa de decisión** — #146 — `renovatio-profile` + `renovatio-decisions` + `DecisionSuggestionService` (LLM) + Steps \"Target\" y \"Decisions\" en la UI. Defaults = comportamiento actual byte a byte. Depende de F0.\n- [ ] **F2 · semantic-ir + TargetEmitter SPI** — #147 — separar análisis de emisión; promover `@CobolDataIntent` a modelo neutral; `JavaEmitter` envuelve la generación actual. Refactor con red de caracterización. Depende de F1.\n- [ ] **F3 · Arquitectura como transformación** — #148 — `renovatio-architecture`: `SemanticIR → TargetModel` por estilo (transaction-script + hexagonal); preview real del layout. Depende de F2.\n- [ ] **F4 · PersistenceStrategy pluggable** — #149 — clasificador de accesos a datos + estrategias (JPA / spring-data-jdbc / in-memory) elegibles por fuente; Step \"Persistence\". Depende de F2.\n- [ ] **F5 · Segundo lenguaje destino** — #150 — `NodeEmitter` (o Python) + catálogo de patrones idiomáticos + una `PersistenceStrategy`. Validación de que el corte es multi-target. Depende de F3 y F4.\n- [ ] **F6 · Residuo e impedancia fina (LLM)** — #151 — naming idiomático, javadoc/docstring explicativo, estructuración de control flow (plan LLM + pass determinista + `ControlFlowPlanGate`), diff semántico en la UI. Depende de F2.\n- [ ] **F7 · renovatio-jcl** — #153 — orquestación batch: JCL steps, `COND`, datasets, utilidades (`SORT`/`IDCAMS`) hacia el destino elegido (`spring-batch` primero). Fase futura. Depende de F2 y F3.\n- [ ] **F8 · Perfiles y políticas de decisión reutilizables** — #154 — templates de `migration-profile` y catálogo de políticas aplicables entre proyectos, con herencia/override. Fase futura. Depende de F1.\n\n## Invariantes (todas las fases)\n\n- **Determinismo primero, LLM solo en el residuo.** El LLM propone plan / naming / clasificación; nunca tipea el archivo final.\n- **Defaults = comportamiento actual** en cada fase → nunca se rompe lo que funciona.\n- **Tests de caracterización** (harness de #122) como red antes de cada refactor y condición de merge.\n- **Toda salida LLM** validada contra JSON schema, con temperatura 0, cache determinista y registro de prompt/modelo/versión/rationale.\n- **Humano en el loop** en toda decisión de baja confianza.\n- **YAGNI**: 2 arquitecturas (no 6), 2-3 estrategias de persistencia (no todas), 1 segundo lenguaje (no 2).\n\n## Gaps de producto que cubre el epic\n\n| Área | Nuevo |\n|---|---|\n| Libs | `renovatio-semantic-ir`, `renovatio-profile`, `renovatio-decisions`, `renovatio-architecture`, `renovatio-persistence`, `renovatio-emitter-{node,python}` |\n| API | endpoints de profile / decisions / data-accesses / profile:effective; (jobs persistidos y SSE vienen de #130) |\n| Web app | Steps Target / Decisions / Persistence; preview de layout; vista de arquitectura; diff semántico; badge IA + rationale |\n| CLI | `profile init`, `decisions list/set`, `generate --profile` (sobre #130 F1) |\n| Providers | lado simétrico de **emisión** (`renovatio-provider-python` hoy solo analiza) |\n\n## Gobernanza\n\nBajo Agora. F0 es spike (recomendación). F1–F6: una fase = un ciclo spec→plan→implementación, spec-driven + TDD. Swarm nuevo o dentro de `002-ai-modernization`.\n\n---\n🤖 Generated with [Claude Code](https://claude.com/claude-code)\n\n","createdAt":"2026-08-31T22:46:28Z","labels":[{"id":"LA_kwDOPowW0s8AAAACJUCmtw","name":"enhancement","description":"New feature or request","color":"a2eeef"}],"milestone":null,"number":152,"state":"OPEN","stateReason":"","title":"Epic · Motor de migración parametrizable por decisiones (multi-target, multi-arquitectura, LLM-assisted)","updatedAt":"2026-08-31T22:58:10Z","url":"https://github.com/Modern-Ash/renovatio/issues/152"}

## Standard error

    (empty)
