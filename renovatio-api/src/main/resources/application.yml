server:
  port: 8080

spring:
  datasource:
    url: jdbc:h2:file:./data/renovatio-db;AUTO_SERVER=TRUE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 200MB
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
  h2:
    console:
      enabled: true
      path: /h2-console

renovatio:
  reusable-assets:
    root: ${user.home}/.renovatio
  api:
    async:
      core-pool-size: 4
      max-pool-size: 8
      queue-capacity: 100
