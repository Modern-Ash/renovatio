[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "renovatio-provider-python"
version = "0.0.1"
description = "Renovatio Python language provider: COBOL runtime mirror and Python code generation for COBOL-to-Python migration."
requires-python = ">=3.10"
dependencies = ["jinja2>=3.0", "jsonschema>=4.0"]

[project.optional-dependencies]
test = ["pytest>=7.0"]

[tool.hatch.build.targets.wheel]
packages = ["src/renovatio_python"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
